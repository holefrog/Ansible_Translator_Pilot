# Translator-Pilot Settings Configured dynamically via Ansible deployment template

[provider]
stt = "groq_whisper"
translate = "groq_llm"
tts = "azure_speech"

[stt.groq_whisper]
api_key = "{{ groq_api_key }}"
model = "{{ groq_model }}"

[stt.gemini]
api_key = "{{ gemini_api_key }}"
model = "{{ gemini_model }}"

[translate.groq_llm]
api_key = "{{ groq_api_key }}"
model = "{{ groq_llm_model }}"
system_prompt = """{{ llm_system_prompt | trim }}"""
user_prompt = """{{ llm_user_prompt | trim }}"""

[translate.gemini]
api_key = "{{ gemini_api_key }}"
model = "{{ gemini_model }}"
system_prompt = """{{ llm_system_prompt | trim }}"""
user_prompt = """{{ llm_user_prompt | trim }}"""

[tts.azure_speech]
api_key = "{{ azure_speech_key }}"
region = "{{ azure_speech_region }}"
voice = "{{ azure_speech_voice }}"

[tts.gemini_tts]
voice = "{{ gemini_tts_voice }}"

[retry]
max_retries = {{ retry_max_retries }}
base_delay = {{ retry_base_delay }}
backoff_factor = {{ retry_backoff_factor }}
max_delay = {{ retry_max_delay }}

[align]
warning_threshold_ratio = {{ align_warning_threshold_ratio }}
